FROM python:3.12-slim
WORKDIR /app
COPY . /app
RUN pip install --no-cache-dir uv && uv pip install --system .
EXPOSE 8501
CMD ["streamlit", "run", "apps/streamlit_demo/app.py", "--server.port", "8501", "--server.address", "0.0.0.0"]
